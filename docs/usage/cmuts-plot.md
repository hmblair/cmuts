
## Purpose

`cmuts plot` may be used to plot one or more reactivity profiles stored in HDF5 files.

## Usage

The general syntax is
```bash
cmuts plot \
  --files FILE1 FILE2 ... \
  --datasets DS1 DS2 ... \
  --labels LABEL1 LABEL2 ...
```
and an example can be found under `./examples/plot`.
