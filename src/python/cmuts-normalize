#!/usr/bin/env python3
"""Thin entry point that delegates to ``cmuts.normalize.__main__``.

The implementation lives in ``src/python/cmuts/normalize/__main__.py`` so it
can also be invoked as ``python -m cmuts.normalize``.
"""

from __future__ import annotations

from cmuts.normalize.__main__ import main

if __name__ == "__main__":
    main()
