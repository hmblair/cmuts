#compdef cmuts

# AUTOMATICALLY GENERATED by `shtab`


_shtab_cmuts_commands() {
  local _commands=(
    "align:"
    "core:"
    "generate:"
    "normalize:"
    "plot:"
    "test:"
    "visualize:"
  )
  _describe 'cmuts commands' _commands
}

_shtab_cmuts_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-v,--version}"[Show version]"
  "--log[Log file]:log:_files"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_defaults_added=0

_shtab_cmuts_align_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--fasta[Reference FASTA]:fasta:_files -g '*.(fa|fasta|fna)'"
  "--output[Output directory]:output:_files"
  "--overwrite[Overwrite existing output]"
  "--rebuild[Rebuild index files]"
  "--verbose[Verbose output]"
  "--threads[Number of threads]:threads:"
  "--barcodes[Barcode file]:barcodes:_files -g '*.(csv|tsv|txt)'"
  "--trim-5[5\' adapter sequence]:trim_5:"
  "--trim-3[3\' adapter sequence]:trim_3:"
  "--local[Local alignment mode]"
  "--pairs[Paired-end reads]:pairs:_files -g '*.(fastq|fastq.gz|fq|fq.gz)'"
  "(*)::Input reads:_files -g '*.(fastq|fastq.gz|fq|fq.gz|bam)'"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_align_defaults_added=0

_shtab_cmuts_core_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  {-o,--output}"[The output HDF5 file.]:output:_files -g '*.(h5)'"
  {-f,--fasta}"[The reference FASTA file.]:fasta:_files -g '*.(fa|fasta|fna)'"
  "--overwrite[Overwrite an existing HDF5 file.]"
  "--rebuild[Rebuild all index files.]"
  {-c,--compression}"[Compression level of the HDF5 output (0-9).]:compression:"
  "--print-every[How often (in seconds) to print statistics.]:print_every:"
  {-v,--verbose}"[Enable verbose (debug) logging to .cmuts.log file.]"
  "--pairwise[Compute pairwise modification counts.]"
  "--tokenize[Tokenize the reference sequences.]"
  "--token-map[Comma-separated non-negative integer tokens for A,C,G,U in order, used by --tokenize (e.g. \"4,5,6,7\"). T and U share the fourth value. Defaults to 0,1,2,3.]:token_map:"
  "--min-mapq[Mapping quality threshold for alignment processing.]:min_mapq:"
  "--min-phred[PHRED score threshold for base processing.]:min_phred:"
  "--min-length[Skip reads shorter than this length.]:min_length:"
  "--max-length[Skip reads longer than this length.]:max_length:"
  "--max-hamming[The maximum number of mismatches, insertions, and deletions in a processed read.]:max_hamming:"
  "--secondary[Consider secondary alignments for processing as well.]"
  "--downsample[Process at most this many quality-passing reads per reference (caps usable depth and avoids over-processing). Reads failing the quality filters do not count toward the limit.]:downsample:"
  "--ignore-bases[Do not count mismatches or deletions occurring at these bases. Pass as a single string.]:ignore_bases:"
  "--max-indel-length[The longest indels to consider.]:max_indel_length:"
  "--quality-window[Check the quality of each base in a window of this size around each base.]:quality_window:"
  "--collapse[Collapse modifications within this distance of each other in a given read.]:collapse:"
  "--chunk-size[The number of references to process at a time per thread.]:chunk_size:"
  "--no-mismatches[Do not count mismatches as modifications.]"
  "--no-insertions[Do not count insertions as modifications.]"
  "--no-deletions[Do not count deletions as modifications.]"
  "--no-reverse[Ignore reverse-complemented reads.]"
  "--only-reverse[Use only reverse-complemented reads.]"
  "--uniform-spread[Uniformly spread out ambiguous deletions.]"
  "--no-spread[Do not spread ambiguous deletions.]"
  "--disable-ambiguous[Disable the ambiguous deletion detection algorithm, relying on the deletion provided by the alignment.]"
  "--no-match-filter[Do not filter matches based on their PHRED base score.]"
  "--no-insertion-filter[Do not filter insertions based on their PHRED base score.]"
  "--no-deletion-filter[Do not filter deletions based on their PHRED base score.]"
  "--threads[Number of MPI processes]:threads:"
  "(*)::Input alignments:_files -g '*.(sam|bam|cram)'"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_core_defaults_added=0

_shtab_cmuts_generate_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--length[The length of the reference sequences.]:length:"
  "--queries[The number of queries to generate per reference.]:queries:"
  "--references[The number of references to generate.]:references:"
  "--seed[Random seed for reproducibility. If not provided, uses time-based seed.]:seed:"
  "--out-fasta[The file to store the references in.]:out_fasta:_files -g '*.(fa|fasta|fna)'"
  {-o,--out}"[The output file for alignments (SAM, BAM, or CRAM).]:out:_files -g '*.(sam|bam|cram)'"
  "--out-h5[The file to store the expected modifications in.]:out_h5:_files -g '*.(h5)'"
  "--min-mapq[The minimum quality to consider a read.]:min_mapq:"
  "--min-phred[The minimum quality to consider a base.]:min_phred:"
  "--min-length[The smallest query sequences to consider when counting modifications.]:min_length:"
  "--max-length[The longest query sequences to consider when counting modifications.]:max_length:"
  "--max-indel-length[Skip indels longer than this.]:max_indel_length:"
  "--collapse[The minimum number of bases between modifications to consider them consecutive.]:collapse:"
  "--quality-window[The number of neighbouring bases to consider when calculating PHRED scores.]:quality_window:"
  "--no-mismatches[Do not count mismatches as modifications.]"
  "--no-insertions[Do not count insertions as modifications.]"
  "--no-deletions[Do not count deletions as modifications.]"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_generate_defaults_added=0

_shtab_cmuts_normalize_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--mod[Single-group mode\: HDF5 paths for the modified condition (replicates are summed). Mutually exclusive with --groups.]:mod:"
  "--nomod[Single-group mode\: HDF5 paths for the unmodified control. Mutually exclusive with --groups.]:nomod:"
  "--group[Single-group mode\: name for the output HDF5 group. Mutually exclusive with --groups.]:group:"
  "--groups[Multi-group mode\: path to a TOML file describing the groups. See the documentation for the file format.]:groups:"
  "--independent-norm[Normalize each group independently rather than pooling across groups. Has no effect in single-group mode.]"
  "--fasta[FASTA file of reference sequences.]:fasta:_files -g '*.(fa|fasta|fna)'"
  {-o,--out}"[The output HDF5 file to write to.]:out:_files -g '*.(h5)'"
  "--overwrite[Overwrite an existing HDF5 file (the whole file, not just the group).]"
  "--clip-below[Clip reactivity values below this threshold (e.g. 0).]:clip_below:"
  "--clip-above[Clip reactivity values above this threshold (e.g. 1).]:clip_above:"
  "--blank-5p[NaN out this many bases on the 5\' end.]:blank_5p:"
  "--blank-3p[NaN out this many bases on the 3\' end.]:blank_3p:"
  "--blank-cutoff[NaN out any positions with less than this many reads.]:blank_cutoff:"
  "--norm[Normalization method\: ubr (90th percentile), raw (none), outlier (2-8\%).]:norm:(ubr raw outlier)"
  "--no-insertions[Do not use insertions to compute the reactivity profile.]"
  "--no-deletions[Do not use deletions to compute the reactivity profile.]"
  "--sig[The significance level for correlation plots.]:sig:"
  ":The HDF5 file containing the input data.:_files -g '*.(h5)'"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_normalize_defaults_added=0

_shtab_cmuts_plot_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--files[Path to HDF5 file(s)]:files:_files -g '*.(h5)'"
  "--datasets[Dataset name in HDF5 file(s)]:datasets:"
  "--labels[Labels to use on plots]:labels:"
  "--index[Index of reactivity in the dataset.]:index:"
  "--trim-5p[Trim this many bases from the 5\' end of the reactivity data.]:trim_5p:"
  "--trim-3p[Trim this many bases from the 3\' end of the reactivity data.]:trim_3p:"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_plot_defaults_added=0

_shtab_cmuts_test_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--quick[Run quick tests only]"
  "--cram[Run CRAM-specific tests only]"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_test_defaults_added=0

_shtab_cmuts_visualize_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--file[Path to HDF5 file]:file:_files -g '*.(h5)'"
  "--dataset[Dataset name in HDF5 file]:dataset:"
  "--cif[Path to .cif structure file]:cif:_files -g '*.(cif|mmcif)'"
  "--fasta[Path to .fasta file of the sequence which was chemically probed.]:fasta:_files -g '*.(fa|fasta|fna)'"
  "--bin[Path to ChimeraX binary.]:bin:"
  "--index[Index of reactivity in the dataset.]:index:"
  "--chain[Chain of the structure to color. All if none is specified.]:chain:"
  "--color[The ChimeraX color to use for highly reactive bases.]:color:"
  "--trim-5p[Trim this many bases from the 5\' end of the reactivity data.]:trim_5p:"
  "--trim-3p[Trim this many bases from the 3\' end of the reactivity data.]:trim_3p:"
  "--vmin[Clip reactivity values below this threshold.]:vmin:"
  "--vmax[Clip reactivity values above this threshold.]:vmax:"
  "--defattr-out[Path to write the .defattr file (default\: a hidden temp file).]:defattr_out:"
  "--no-chimerax[Don\'t launch ChimeraX\; only write the .defattr file and print the ChimeraX command for the user to run themselves.]"
)

# guard to ensure default positional specs are added only once per session
_shtab_cmuts_visualize_defaults_added=0


_shtab_cmuts() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*' default='*::: :->cmuts'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_cmuts_defaults_added )); then
    if (( ${_shtab_cmuts_options[(I)${(q)one_or_more}*]} +          ${_shtab_cmuts_options[(I)${(q)remainder}*]} +          ${_shtab_cmuts_options[(I)${(q)default}]} == 0 )); then
      _shtab_cmuts_options+=(': :_shtab_cmuts_commands' '*::: :->cmuts')
    fi
    _shtab_cmuts_defaults_added=1
  fi
  _arguments -C -s $_shtab_cmuts_options

  case $state in
    cmuts)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_cmuts-$line[1]:"
      case $line[1] in
        align) _arguments -C -s $_shtab_cmuts_align_options ;;
        core) _arguments -C -s $_shtab_cmuts_core_options ;;
        generate) _arguments -C -s $_shtab_cmuts_generate_options ;;
        normalize) _arguments -C -s $_shtab_cmuts_normalize_options ;;
        plot) _arguments -C -s $_shtab_cmuts_plot_options ;;
        test) _arguments -C -s $_shtab_cmuts_test_options ;;
        visualize) _arguments -C -s $_shtab_cmuts_visualize_options ;;
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_cmuts -N cmuts
else
  # autoload from fpath, call function directly
  _shtab_cmuts "$@"
fi
